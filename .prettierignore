# Build output + caches + local installs — regenerable, never formatted.
dist/
.astro/
node_modules/
package-lock.json
pnpm-lock.yaml

# Shared content/token source of truth (cv.json, tokens.css, schemas). Authored
# to a fixed byte contract; never reformatted here.
jct-core/

# Paraglide-generated runtime + message modules (US-030). Regenerated by the
# paraglide Vite plugin on every dev/build; never committed or formatted.
src/paraglide/

# inlang project machine state (US-030). The inlang SDK git-ignores everything in
# project.inlang/ except settings.json; these generated files are not formatted.
project.inlang/.meta.json
project.inlang/README.md
project.inlang/cache/
